# App Workspace

This directory will hold the user-facing application surface for the reference
DEX.

The app should remain downstream of the ledger and service workflows. Until the
source-aligned Daml and service layers are stable, the app layer stays as
scaffold only.
