-- DEX-side adapter that exposes our MockRegistry through the Splice
-- token-standard test interface (`Splice.Testing.TokenStandard.RegistryApiV2`).
--
-- The upstream `splice-token-standard-test-v2` package tests TradingAppV2
-- by having a registry implement that interface and then driving the
-- trading workflow through it. Doing the same for our DEX validates
-- that our matched-trade workflow is wired into the V2 surface in
-- exactly the way upstream expects.
--
-- This module is the smallest possible RegistryApi instance: an
-- AllocationFactory + SettlementFactory pair backed by the existing
-- `CantonDex.Testing.MockRegistry` templates. We only fill in the
-- factory methods our DEX flow exercises; TransferFactory and the
-- choice-context helpers stay as `error` placeholders since the
-- MatchedTrade flow does not touch them.

module CantonDex.TokenStandardHarness.DexRegistry where

import Daml.Script

import Splice.Api.Token.AllocationV2 qualified as V2
import Splice.Api.Token.AllocationInstructionV2 qualified as V2
import Splice.Api.Token.TransferInstructionV2 qualified as V2
import Splice.Api.Token.MetadataV1

import Splice.Testing.Utils
import Splice.Testing.TokenStandard.RegistryApiV2

import CantonDex.Testing.MockRegistry qualified as Mock

-- | A token-standard-compatible registry backed by our MockRegistry
-- factories. The DEX operator sees both factories as observers so the
-- venue can exercise on them.
data DexRegistry = DexRegistry
  with
    admin : Party
    allocationFactory : ContractId V2.AllocationFactory
    settlementFactory : ContractId V2.SettlementFactory

-- | Boot a `DexRegistry` for an admin party that has the listed parties
-- as observers on both factories. Matches the shape `AmuletRegistryV2`
-- uses for its setup.
initialize : Party -> [Party] -> Script DexRegistry
initialize admin users = do
  allocFactoryCid <- submit admin $
    createCmd Mock.MockAllocationFactory with admin; users
  settleFactoryCid <- submit admin $
    createCmd Mock.MockSettlementFactory with admin; users
  pure DexRegistry with
    admin
    allocationFactory = toInterfaceContractId allocFactoryCid
    settlementFactory = toInterfaceContractId settleFactoryCid

instance RegistryApi DexRegistry where
  getAllocationFactory registry arg = pure EnrichedFactoryChoice with
    factoryCid = registry.allocationFactory
    arg
    disclosures = mempty

  getSettlementFactory registry arg = pure EnrichedFactoryChoice with
    factoryCid = registry.settlementFactory
    arg
    disclosures = mempty

  -- The MatchedTrade flow does not exercise TransferFactory or the
  -- per-allocation choice-context helpers. Leave them as errors so a
  -- future caller that does reach for them fails loud rather than
  -- silently with a stub. The upstream `splice-token-standard-test-v2`
  -- registries follow the same posture for unused choices.
  getTransferFactory _ _ = error "DexRegistry: TransferFactory not implemented"
  getAllocation_WithdrawContext _ _ _ = error "DexRegistry: Allocation_Withdraw context not implemented"
  getAllocation_CancelContext _ _ _ = error "DexRegistry: Allocation_Cancel context not implemented"
  getTransferInstruction_AcceptContext _ _ _ = error "DexRegistry: TransferInstruction_Accept context not implemented"
  getTransferInstruction_RejectContext _ _ _ = error "DexRegistry: TransferInstruction_Reject context not implemented"
  getTransferInstruction_WithdrawContext _ _ _ = error "DexRegistry: TransferInstruction_Withdraw context not implemented"
