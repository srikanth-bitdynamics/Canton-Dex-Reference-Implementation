module CantonDex.Tests.SourceAlignment where

import CantonDex.DexApp.AllocationExtensionsV2
import CantonDex.DexApp.OTCTradeV2
import CantonDex.Instrument.RegistryWorkflows
import CantonDex.Testing.OTCTradeV2Backend

-- This test module is intentionally empty for now.
--
-- The first real test tranche should validate that the local implementation
-- remains aligned with the exact workflow surfaces from:
--   * TradingAppV2
--   * the registry workflow documentation
--   * PR 5333 allocation extensions

sourceAlignmentAnchors =
  ( sourceAllocationExtensionAnchor
  , sourceHoldingAnchor
  , registryWorkflowModuleNote
  , sourceTradeQueryAnchor
  )
